% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wavelets.R
\name{wt_forward_mat}
\alias{wt_forward_mat}
\title{Forward Wavelet Transform for Matrix}
\usage{
wt_forward_mat(y_mat, wf = "la8", J = NULL, boundary = "periodic")
}
\arguments{
\item{y_mat}{Matrix of size P x M where P is the number of time points and M is the number of channels}

\item{wf}{Character string specifying the wavelet filter (default: "la8")}

\item{J}{Integer specifying the number of decomposition levels (default: NULL, auto-determined)}

\item{boundary}{Character string specifying boundary condition (default: "periodic")}
}
\value{
List of length M, where each element is the result of wt_forward_1d() for the corresponding column
}
\description{
Performs forward wavelet transform on each column of a matrix independently.
Useful for multivariate functional data where each column represents a different channel.
}
\details{
This function applies wt_forward_1d() to each column of the input matrix,
making it suitable for multivariate functional data analysis.
}
\examples{
y_mat <- matrix(rnorm(128), nrow = 64, ncol = 2)
result <- wt_forward_mat(y_mat, wf = "la8", J = 4)
}
